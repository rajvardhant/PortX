-- data.sql
-- NOTE: User, Vehicle, and Driver seed data is now handled by DataInitializer.java
-- which uses the real PasswordEncoder bean so passwords are always correctly hashed.
-- This file is intentionally left minimal to avoid duplicate-key errors on restart.

-- You can add any additional SQL here if needed (e.g., lookup table data).
